import { app } from "../src/app.js";

// Optional Vercel serverless entry point. For the recommended Ubuntu/Docker
// deployment, apps/api/src/server.ts remains the long-running process entry.
export default app;
